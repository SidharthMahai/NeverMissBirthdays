# Frontend -> Vercel API proxy
# Optional for local dev only. In production, frontend uses same-origin /api automatically.
VITE_API_BASE_URL=
# Local dev proxy target for Vite (avoids CORS on localhost):
VITE_DEV_APPS_SCRIPT_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
# Optional: sent in request body; must match Apps Script hardcoded key if check is enabled.
VITE_APPS_SCRIPT_CLIENT_KEY=replace-with-random-string

# Vercel serverless function -> Apps Script
APPS_SCRIPT_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
# Must match HARDCODED_CLIENT_KEY in apps-script/Code.gs
APPS_SCRIPT_CLIENT_KEY=replace-with-random-string
